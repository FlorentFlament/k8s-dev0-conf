apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: yogishow
spec:
  rules:
  - host: {{ .Values.hostname }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: yogishow
            port:
              number: 80
